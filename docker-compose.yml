version: "3"
services:
  app:
    build:
      args:
        user: zarea
        uid: 1000
      context: ./
      dockerfile: Dockerfile
    image: accendo
    container_name: accendo-app
    restart: unless-stopped
    working_dir: /var/www/
    volumes:
      - ./:/var/www
    networks:
      - accendo

  db:
    image: mysql:5.7
    container_name: accendo-db
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_USER: ${DB_USERNAME}
      SERVICE_TAGS: dev
      SERVICE_NAME: mysql
    volumes:
      - ./docker-compose/mysql:/docker-entrypoint-initdb.d
    networks:
      - accendo

  nginx:
    image: nginx:alpine
    container_name: accendo-nginx
    restart: unless-stopped
    ports:
      - 8000:80
    volumes:
      - ./:/var/www
      - ./docker-compose/nginx:/etc/nginx/conf.d/
    networks:
      - accendo

networks:
  accendo:
    driver: bridge
